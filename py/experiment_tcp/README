here we test the following setting.

1 sender sends 10MB of data to receiver.
both are located in same rack
we measure flow completion time.

we vary the size of the queue.

we show that small buffer sizes hurt flow completion time


flow_id start_time unused unused size unused unused src dest
0 2.0 -1 -1 10485760 -1 -1 0 1

